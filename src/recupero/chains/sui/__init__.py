"""Sui mainnet chain adapter (roadmap-v4: Sui live transfer coverage).

The address codec (``chains/move_address.py``) shipped earlier; this package adds
the live transfer-fetching layer (``client`` + ``adapter``) verified against real
``fullnode.mainnet.sui.io`` JSON-RPC responses.
"""
